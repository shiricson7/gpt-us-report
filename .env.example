NEXT_PUBLIC_SUPABASE_URL=YOUR_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Required for the "AI 다듬기" button (server-side only)
OPENAI_API_KEY=YOUR_OPENAI
# Optional
OPENAI_MODEL=gpt-5.2

# Server-only: used to encrypt sensitive fields like RRN before storing in DB.
# Any strong random string is OK (rotate carefully; existing ciphertext will be unreadable).
RRN_ENCRYPTION_SECRET=YOUR_RANDOM_STRING
